#!/usr/bin/bash

build() {
    add_runscript
}

help() {
    cat <<'EOF'
Experimental early driver_override quarantine for one explicit PCI BDF.
This hook is not hardware-validated and is not installed by gpu-triage.
EOF
}
